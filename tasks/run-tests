;; -*- mode: fennel; -*- vi:ft=fennel

(local {: run-tests}
  (require :lib.fennel-test))

(local tests
  [:test.json-test
   :test.readers-test
   :test.http-parser-test
   :test.headers-test])

(run-tests tests)
